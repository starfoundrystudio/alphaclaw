# Ignore everything by default.
*

# Whitelist specific files/dirs.
!workspace/
!workspace/**
workspace/.openclaw/
workspace/.openclaw/**
!gogcli/
gogcli/*
!gogcli/state.json
db/
db/**
!skills/
!skills/**
!hooks/
!hooks/transforms/
!hooks/transforms/**
!pages/
!pages/**
# OpenClaw 2026.6.10+ stores cron and other runtime state in SQLite.
# Runtime databases, WAL files, and legacy cron files must stay local.
state/
state/**
cron/
cron/**
agents/*/agent/*.sqlite
agents/*/agent/*.sqlite-*
!openclaw.json
!.gitignore
