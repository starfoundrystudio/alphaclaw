# OpenClaw 2026.9.x upgrade — W6 closeout

Status: complete 2026-09-20 on branch `codex/openclaw-2026.9.4-upgrade`
(pinned `openclaw@2026.9.5` since `c631279`). W6 is "tests, docs, and
release hygiene" in the upgrade plan; this document closes each bullet and
carries the S0–S2 impact-matrix closeout that gates G1/G3.

## Outcome

| W6 bullet | Result |
| --- | --- |
| Tests pinning 7.1 shapes; canaries first | The two canaries (`openclaw-thinking`, `model-catalog-bootstrap`) were the first to break on the 2026.9.5 retarget and were fixed in `c631279` (loader picks the chunk that exports `listThinkingLevelOptions` by name; generator reads OpenClaw's bundled plugin-manifest catalogs). Tests that still name 7.1 fixtures (`cron-service`, `openclaw-startup-state-repair`, `openclaw-plugin-compat-vault-env`, `openclaw-2026-9-upgrade`) exercise the upgrade path from 7.1 shapes and stay. Full vitest 166 files / 1,488 tests. |
| Replace hardcoded `2026.7.1` strings; rewrite `docs/fork-deviations.md` (Codex route) | `f4b3527`: comments in `gateway.js`, `codex-broker-service.js`, `chat-ws.js`, the two migrations and the two specs now describe the pinned 2026.9.x line; the Codex-route deviation is rewritten for canonical `openai/*` with the runtime on `models.providers.openai.agentRuntime` (W2 B2). The standalone migration tooling was removed in W0 (`7WXFrnH9bZSA`). |
| TeamYou memory plugin against 9.x | `@teamyou/openclaw-memory` 0.3.0 typechecks and passes its 11 tests against `openclaw@2026.9.5` types (installed `--no-save`, tree restored); manifest carries `id`, `setup`, `contracts`, `toolMetadata`, `configSchema`, `uiHints`; it loaded on both G2 instances at 9.4 and 9.5 (7-plugin Gateway) and the activation gate held (T7). No `assets/icon.png` (cosmetic, F5). No rebuild needed. |
| CLI-runtime continuity | Live on G2 (T7): three-turn history and codeword recall across the upgrade and two restarts, TeamYou memory activation gate. Not run live: Claude login/adoption without the projects symlink, an MCP/tool turn, managed Codex across the HOME change and `CODEX_HOME` unchanged (no subscription login was made on the disposable instance). Unit coverage exists (A4/B5/H2 rows below); the live part moves to G3's internal-instance soak. |
| S0–S2 rows closed | All 46 tabulated rows have a mechanism, a test or a G2 test, and a delivery path (table below). None UNMAPPED. Twelve are "implemented, weakly evidenced"; their dispositions are listed. |
| Existing-managed-instance delivery path per row | Every durable-behaviour row delivers through one of: the clawctl host upgrade script, the two `2026-09-*` AlphaClaw migrations, guarded Doctor, or the managed boot/reconcile path. G2's 7.1→9.4 crossing exercised all four (ten runs on instance 01, one on 02). Gap: the upgrade script and the two clawctl fixes (finding #1 retired-key writer, finding #8 backup lock files) are on Preview beta bundles only; promotion to the stable pin is a G3 exit condition. |
| Two model refresh lifecycles | Unit: `routes-models` "refreshes hosted metadata and provider inventory through separate OpenClaw commands"; `model-catalog-cache` "uses provider discovery only for an explicit refresh", "marks a fresh memory cache stale when the openclaw version changes", "invalidates the prior cache schema". Live on an upgraded instance: T11 (`source: openclaw`, `restartRequired: false`); the true "hosted download requires restart" state was not induced. |

## Also landed in W6

- `bin/alphaclaw.js` rejects unknown subcommands with exit 2 (row C2 /
  finding #2b), with a spawn test.
- clawctl `fe1b532`: `KillMode=mixed` and `TimeoutStopSec=90` on every
  alphaclaw unit, per 2026.9.5's shutdown guidance; bundle `f62fa09f` pinned
  on Preview beta.
- The retarget assessment (`docs/openclaw-2026.9.5-retarget-assessment.md`)
  records what 2026.9.5 changed; no instance has run 2026.9.5 yet.

## Weakly evidenced rows and their disposition

| Row | Gap | Disposition |
| --- | --- | --- |
| A4, B2, B5, H2, I3 | No Codex or Claude subscription ran through G2 | G3 internal-instance soak (subscription logins exist there) |
| H1, H4 | No fresh 9.x onboarding has run | G3 fresh beta provision |
| H3 | `openclaw onboard --json` output is not parsed | accepted for the release; parse when onboarding is next touched |
| B8 | Agent flagged two raw config writers; on inspection both target other files (gog `config.json`, Composio `config.json`), and `gmail-watch`'s `openclaw.json` write already goes through the shared writer | closed, no change |
| C1 | VACUUM/checkpoint on the shared state DB fails closed on a busy checkpoint, no lease | accepted; Doctor owns the store |
| C2 | unknown-subcommand rejection | closed in W6 |
| D1/D3 | launcher-death orphan (finding #3); 9.5 changed restart semantics | re-run T3 crash case at G3 before deciding the W3 follow-up |
| G1 | prior-cache invalidation on an upgraded instance not asserted in T11 | unit-covered; assert on the G3 crossing |
| F5 | no `assets/icon.png` in the bundled plugins | cosmetic, post-release |

## S0–S2 impact-matrix closeout

Compiled 2026-09-20 from the impact matrix, the W1–W5c closeouts, the G2
runbook and the branch.

| Row | Sev | Status | Evidence (commit / file:line, test, T#) | Delivery path to existing managed instances |
| --- | --- | --- | --- | --- |
| A1 | S0 | implemented | `949f8d9`: `package.json:61` engines `>=24.16.0 <25 \|\| >=26.1.0`; `lib/runtime/node-sqlite-safety.js:44,60`; `tests/runtime/node-sqlite-safety.test.js`. clawctl `ca86248` (Node 26 provisions) + `assets/host/lib/node-runtime.sh:53,56`. `docs/openclaw-2026.9.4-runtime-audit.md` (fleet sweep deferred to per-instance pre-check, plan §10.7). 9.5 engines unchanged (retarget §1). | HUS pre-check (`node-runtime.sh`) per instance at G3/G4; G2 exercised (Node 24.21.0 accepted, run 1). Fleet sweep: 1 of 3 reachable hosts (`mat-starfoundry-1`, Node 22) needs a Node upgrade first; 7 unverified. |
| A2 | S1 | implemented | `ef2aedf` `lib/server/auth-profiles.js:508-528` (`inspectSharedAuthOwnership`, opens existing DB read-only, no schema creation); `tests/server/auth-profiles.test.js:130` "follows OpenClaw's shared-store ownership marker without creating schema". `openclaw-doctor-oauth-guard.js:228` returns null when the DB is absent (no creation). Residual: guard `writeSqliteStore` (`:257`) still `CREATE TABLE IF NOT EXISTS auth_profile_store` on an *existing* legacy-main DB (restore path only). | DOC owns DB creation/migration; G2 T1 `shared-auth-store` step completed. n/a for AlphaClaw state. |
| A4 | S1 | implemented (live evidence partial) | `ef2aedf` `bin/alphaclaw.js:560-574` sets `OPENCLAW_HOME`/`CODEX_HOME`/`OPENCLAW_STATE_DIR` without replacing HOME; `lib/server/openclaw-runtime-env.js:13` `resolveManagedCodexHome`; `tests/bin/alphaclaw.test.js:112-116` entrypoint env capture; `tests/server/openclaw-runtime-env.test.js:63,76` (legacy vs fresh Codex home). G2 T7: codeword recall across upgrade + 2 restarts PASS; **Claude-login/MCP-turn leg and managed-Codex `CODEX_HOME` smoke not run** (runbook §4.8, plan W6 A4 item still open). Which of the two 7.1 `state/openclaw.sqlite` dirs survived was not recorded. | PKG (entrypoint env) + `resolveManagedCodexHome` preserves the legacy Codex home on upgraded instances. G2 exercised (env), continuity test pending. |
| B1 | S0 | implemented | `ef2aedf` `lib/server/agents/shared.js:114,137` roster accessor (reads `agents.list`, writes `agents.entries`, deletes `list`); 11 callers migrated (`agents/*.js`, `auth-profiles.js`, `bootstrap-kickoff.js`, `webhooks.js`, `default-agent.js`, `teamyou-memory-activation.js`, `routes/system.js`, `onboarding/workspace.js`); `tests/server/agents-service.test.js:110` "reads canonical keyed entries and writes only keyed entries"; `tests/cli/openclaw-2026-9-upgrade.test.js` 7.1 `agents.list` fixture through pinned Doctor. G2 run 4: `agents.entries.main`, no `agents.list`. | DOC (`agents.list` → `entries`) via HUS; accessor tolerates both shapes meanwhile. G2 exercised. |
| B2 | S1 | implemented (no live Codex run) | `ef2aedf`/`15ce9a8`: `onboarding/validation.js:17,94` canonicalises `openai-codex/*` → `openai/*` (`tests/server/onboarding-validation.test.js:94`); `openclaw-doctor-oauth-guard.js:19-23` maps `openai-codex:` profile ids (`tests/cli/openclaw-doctor-oauth-guard.test.js:197`); `model-config.js:60` `kHiddenOnboardingProviders={openai-codex}`; `onboarding/openclaw.js` writes `models.providers.openai.agentRuntime.id=codex` (`onboarding-openclaw.test.js:230`). Alias remains in `constants.js:140` for compatibility (W2 closeout). | DOC route migration + existing MIG `2026-07-normalize-codex-plugin-compatibility` / `2026-06-prefer-codex-cli-openai-auth-profile`. **Not exercised in G2** (instance used Vercel AI Gateway, no Codex); Codex smoke is a W6 carry-in. |
| B3 | S2 | implemented | `251c321` `lib/server/managed-defaults-config.js:25-34` strip; `8b8ca91` `lib/server/openclaw-config.js:83-113` `stripRetiredOpenclawConfigKeys` in the shared writer; `tests/server/openclaw-config.test.js:13,47`; `managed-defaults-config.test.js:13`; `onboarding-openclaw.test.js:735` (never writes `memorySearch`). clawctl `455ed6e` `lib/teamyou-install.sh:574-705` stops writing both keys on 2026.9+. G2 finding #1 verified (run 4, timer at 22:40). | DOC migrates → BOOT strips on every write/reconcile; CLAWCTL library fix needed on every existing host (copy `teamyou-install.sh`; stable bundle still writes the keys every 5 min). G2 exercised (hand-patched). |
| B4 | S1 | implemented | W2 closeout: `agents.defaults.models` model-scoped only, no `modelPolicy.allow` written; `onboarding/openclaw.js` deletes `agents.defaults.agentRuntime`; `onboarding-openclaw.test.js:230`. G2 T1/T6: config validates on 9.4. Fresh-provision claude-cli check not run (G3 item 2). | n/a (DOC). G2 exercised via config validation only. |
| B5 | S2 | implemented (live unverified) | `auth-profiles.js:25,1109` `claude-cli` → `anthropic` plugin. No live 9.x `models status` for `claude-cli` (G2 had no Claude subscription). | n/a. Covered by the pending W6 Claude-CLI continuity test / G3. |
| B6 | S2 | implemented | `tests/cli/openclaw-2026-9-upgrade.test.js:186-213` runs pinned `doctor --lint --all --severity-min error --json` over a 7.1 fixture, expects zero findings; writer strip `8b8ca91`. G2: 7.1 lint had no retired-key findings; post-upgrade "no retired config keys" (run 4, T10). Note: the W2 audit missed `plugins.bundledDiscovery` (`web-search-config.js`, fixed `251c321`) and the clawctl writer — both found by G2. | DOC + BOOT strip; CLAWCTL library fix. G2 exercised. |
| B8 | S2 | implemented — partial | `15ce9a8` routed touched writers through `writeOpenclawConfig`; **2 raw `fs.writeFileSync` writers remain**: `lib/server/gmail-watch.js:215`, `lib/server/composio-install.js:270` (bypass the retired-key strip; runbook §4.5 follow-up). `--expect-current-json` concurrency not adopted. | PKG. Not a G2 test target. |
| B9 | S2 | implemented | `15ce9a8` `lib/server/exec-defaults-config.js` uses `openclaw approvals … --json`; `tests/server/exec-defaults-config.test.js:13` "without recreating the retired approvals JSON store". G2 T1: `exec-approvals-json` migration completed, JSON removed. | DOC (`legacy-exec-approvals-json`). G2 exercised. |
| C1 | S0 | implemented (lease clause weak) | `ef2aedf` `auth-profiles.js:508-590,632-686` resolves `config_machine_state["auth.sharedStore"]` (`legacy-main` \| `state-db`), supports `auth_profile_store` and `auth_profile_stores`; `tests/server/auth-profiles.test.js:130,183`; guard `openclaw-doctor-oauth-guard.test.js:330` "shields and restores the state-owned shared auth store". `VACUUM`/`wal_checkpoint` still run in `physicallySanitizeAuthProfileDatabase` (`:696-712`) — fail closed on busy checkpoint with a pending marker (`:717,801-807`), **no `agent_database_leases` use**. G2 T1: `auth.sharedStore={"location":"state-db"}`; T3 crash → watchdog lazy Doctor ran without relocation conflict. | DOC via HUS. G2 exercised. |
| C2 | S1 | implemented | `15ce9a8` deleted `lib/cli/openclaw-startup-state-repair/codex-sidecars.js` (−233), `finalize-openclaw-startup-state` (`bin/alphaclaw.js` −46), tests. G2 T1/T10: transcripts imported to SQLite (`transcript_events` 25 rows on 02). G2 finding #2: HUS still called the retired command → server started in the foreground; clawctl `455ed6e` gates the step on `alphaclaw help`; **AlphaClaw unknown-subcommand guard not added** (`bin/alphaclaw.js` has no rejection). | DOC session-sqlite import; HUS fix in CLAWCTL bundle. G2 exercised (hand-patched script). 9.5 needs explicit `doctor --fix` for legacy conversions — HUS already does (retarget §2). |
| C4 | S1 | implemented | `15ce9a8` removed `lib/server/managed-gateway-device.js` (−323) + test; `routes/pairings.js:36` `plugin-sdk/device-bootstrap`, `:470` `pairing approve --channel --account`; `tests/server/routes-pairings.test.js:92,314,1023`. `cf6be72` bootstrap-URL hand-off (`routes-system.test.js:152`). G2 T1 `device-identity-json` migrated (`paired.json.migrated`), T2 migrated allow entry live, T8 approval via Clawbridge + bootstrap hand-off (run 5). | DOC (`legacy-device-identity-json`, `device-auth`, `channel-pairing`). G2 exercised. |
| C5 | S2 | implemented (retained, re-tested) | `lib/cli/openclaw-startup-state-repair/plugin-index.js:8,104-110,256`; MIG `2026-07-repair-openclaw-plugin-index-conflicts`; `tests/cli/openclaw-startup-state-repair.test.js`. G2: registry refresh "37/65 enabled plugins indexed", `plugins.installedIndex` in `config_machine_state`, reconcile idempotent. | HUS registry refresh + BOOT reconcile. G2 exercised. |
| C6 | S2 | implemented | = B9. | = B9. |
| C7 | S2 | implemented | `auth-profiles.js:265,283` import paths retained; no writer to `auth-profiles.json`/`oauth.json` remains (grep); `auth-profiles.test.js:496` "retires a stale legacy JSON store after migrating its contents to SQLite", `:1220`. | n/a. Not exercised in G2 (no legacy JSON present). |
| D1 | decided | implemented (ownership claim deferred) | `6c77910` `lib/server/openclaw-runtime-env.js:31-37` (`SUPERVISOR_MODE=external`, `SERVICE_REPAIR_POLICY=external`, `NO_RESPAWN`, update refusal) applied in `gateway.js:100`, `auth-profiles.js:184`, `openclaw-runtime-command.js:21` (CLI passthrough), `openclaw-plugin-compat.js:20`, `scripts/generate-model-catalog-bootstrap.mjs:93`; handoff consume `gateway.js:492-508` (`gateway.test.js:331`); `commands.restart=false` `onboarding/openclaw.js:225` + MIG `2026-09-enforce-managed-gateway-ownership-defaults` (`alphaclaw-migrations.test.js:693`). Database ownership claim: deferred per matrix. G2 T3 handoff 2.7 s PASS, T6 `commands.restart=false`; **finding #3** launcher-death orphan open (W3 follow-up); 9.5 restart semantics changed → re-test T3 at G3 (retarget §2, §6.1). | PKG (env) + MIG (ran in G2 run 1). G2 exercised. |
| D2 | S2 | implemented | `gateway.js:320-335` `/startupz` (`status:"started"`) then `/readyz`, log fallback; `gateway.test.js:635,653`. G2 run 4/T3: both 200. | PKG. G2 exercised. |
| D3 | S3 (asked) | implemented | `watchdog.js:34,1147` exit 78 terminal refusal (`watchdog.test.js:743`); `gateway.js:567,764` `startupConfigRefusal`. G2 finding #3: exit 78 correctly not looped, but the orphan Gateway stays unsupervised — open. | PKG. G2 exercised (launcher-kill case). |
| D4 | S2 | implemented | `gateway.js:880` `restartGatewayLight` + handoff path; `commands.restart=false`. G2 T3 in-process `openclaw gateway restart` PASS ("restart request sent to externally supervised process"). | MIG for `commands.restart`. G2 exercised. |
| D8 | S2 | implemented (no live rate test) | `15ce9a8` `cron-service.js:375,488` bulk `cron.runs`; `use-dashboard-launcher.js:23` `kActiveDevicePollMs=3000`; `general/use-general-tab.js:88` 3000. `cron-service.test.js` updated in `15ce9a8` (no 9.4-named case). G2: no 30/min errors observed; not probed. | PKG. |
| D9 | S2 | implemented | `onboarding/openclaw.js:111-114` `--gateway-auth token --gateway-token-ref-env OPENCLAW_GATEWAY_TOKEN` (`onboarding-openclaw.test.js:31`); `routes/system.js:189`; `cf6be72` `openclaw dashboard --no-open --json` bootstrap (`routes-system.test.js:152`). G2: 7.1 env-ref token survived Doctor (lint: plaintext warning only); T8 hand-off PASS. | n/a (existing token config untouched). G2 exercised. |
| E1 | decided | implemented (backup fix not on stable pin) | `e044721` removed git-sync (`bin/alphaclaw.js` −295, `lib/cli/git-sync.js`, `openclaw-config-restore.js`, sidebar git panel, GitHub routes); `internal-files-migration.js:4` removes `hourly-git-sync.sh`. Backups = clawctl export; G2 finding #8 (2026.9 lock files) fixed clawctl `40696a4`/`78d0480` (bundle `fed9a46b`). G2 T10 restore crossing PASS. | PKG + BOOT (`internal-files-migration`); CLAWCTL for the export fix — **G3/G4 blocker until the stable pin carries it** (every upgraded instance loses state backups otherwise). |
| E2 | S2 | G2-covered (T10) + deferred primitive | Export = clawctl `alphaclaw-backup-export.sh` (SQLite online-backup API incl. WAL; retarget §2); `openclaw backup create` as primitive deferred (plan §7). G2 T10: 7.1 snapshots → fresh 7.1 → 9.4 PASS; post-upgrade state snapshot `56038e31` with the fixed export. | CLAWCTL bundle (not stable). Restore drill with npm-installed provider route pending (backup plan rev 3.28, G3 item 3). |
| E3 | S2 | implemented (retired) | `2753104` removed `import-scanner.js`, `portable-*-import.js`, `prepare/publish-openclaw-migration.sh`, import UI + tests (plan W6 [E3]: removed, not adapted). | n/a. |
| F1 | S1 | implemented | `ef2aedf` regenerated manifest (Codex `kind:"plugin"`, `pluginId` `lib/openclaw-compatibility.manifest.json:535-542`); `c631279` regenerated at 2026.9.5 (97 plugins); `openclaw-plugin-compat.js:501-506` keyed by plugin id; `tests/cli/openclaw-plugin-compat.test.js`, `model-catalog-bootstrap.test.js` canary. G2 runs 2–10 reconcile clean on 9.4. | HUS `reconcile-openclaw-plugins` + BOOT reconcile (`2136ef9` retry). G2 exercised. |
| F2 | S1 | implemented | `openclaw-plugin-compat.js:588,595` `--accept-capabilities`; `onboarding/index.js:143` preinstall codex; `generate-model-catalog-bootstrap.mjs:485`. G2: non-interactive installs (cohere, meta, opencode-go, …) succeeded. | HUS/BOOT reconcile. G2 exercised. |
| F3 | S2 | implemented | HUS order Doctor → reconcile → Doctor (clawctl `alphaclaw-host-upgrade.sh:809-858`); `lib/server/startup.js:62` reconcile after doctor repair. G2 exercised every run. 9.5 decoupled plugin updates → re-verify "already installed" detection at G3 (retarget §6.2). | HUS + BOOT. |
| F4 | S1 | implemented | `routes/system.js:141` `plugin-sdk/secret-ref-runtime`; `lib/server/openclaw-plugin-sdk-contract.js:13` boot-time subpath check (`openclaw-plugin-sdk-contract.test.js:7`). G2 T8 token resolution worked. | PKG. G2 exercised. |
| F5 | S2 | implemented (icon missing; 9.5 deprecations unchecked) | `tests/server/usage-tracker-plugin.test.js:6` "OpenClaw 2026.9.4 hook contract" (`llm_output`, `tool_result_persist`); `agent-vault.test.js:1448,1464` (`before_agent_finalize`, `reply_payload_sending`); `lib/plugin/*/openclaw.plugin.json` present. **No `assets/icon.png` in either bundled plugin.** TeamYou memory plugin: loaded on 9.4 (8 plugins) and T7 activation gate PASS; async-storage deprecation warnings to check at G3 (retarget §6.5). | PKG. G2 exercised (hooks fired live). |
| G1 | S1 | implemented | Generator probes with and without `--refresh` (`generate-model-catalog-bootstrap.mjs:314-321`) + bundled-catalog reader (`:228`, `f7d3cc3`, `c631279`); runtime `model-catalog-cache.js:24-36,68-78` `providerOutcomes`/`refreshFailed`; `model-catalog-cache.test.js:638`, `routes-models.test.js:182`. G2 T11 PASS (`source: openclaw`). | BOOT cache-schema v2 invalidation (`model-catalog-cache.test.js:559`). G2: post-upgrade `/api/models` served `source: cache` then `openclaw` on refresh; prior-7.1-cache invalidation was not explicitly asserted on the instance (weak). |
| G2 | S2 | implemented | `model-catalog-cache.js:107-130` row normalisation; `e527110` `accessModes.*.providers[].models` merge (G2 finding #4, verified run 6); `tests/frontend/model-catalog.test.js`, `routes-models.test.js`. | PKG. G2 exercised. |
| G3 | S1 | implemented | `lib/server/openclaw-thinking.js:5,14-57,103` prefers the chunk exporting `listThinkingLevelOptions` by name (`c631279` 9.5 fix); `thinking-levels.js:11` `ultra`; `tests/server/openclaw-thinking.test.js:17` runs against the installed dist (canary, green at 9.5). | PKG. Not a live G2 check. |
| G4 | S1 | implemented | `15ce9a8` `cost-utils.js:215-237` `extractPricingFromPluginManifests` (`dist/extensions/*/openclaw.plugin.json`); `cost-utils.test.js:33`. Usage/cost figures not checked live in G2. | PKG. |
| G5 | S2 | implemented (W5c) | `c0171be` `agents.entries.<id>.model.primary/fallbacks`, `thinkingDefault`, `fastModeDefault`, `tools`, `skills` (`agents-service.test.js:3230`); no `modelPolicy.allow` written. G2 T11 `/api/models/status`; finding #4 fixed. | PKG (optional canonical fields, W5c closeout). G2 exercised. |
| G6 | S2 | implemented | `model-config.js:64-97` `kKnownOnboardingModels` refreshed (opus-4-8, sonnet-4-6, gpt-5.6-sol); GPT-5.5 denylist `7e39d5b`/`dfc8726` (`lib/server/model-denylist.js`, applied at pack time + `/api/models*`); bootstrap regenerated at 9.5 (1,446 models). G2 finding #6 verified run 8. | PKG. Existing configs referencing GPT-5.5 are refused on write (400), not rewritten. G2 exercised. |
| G7 | S2 | implemented | `lib/server/agent-vault/model-provider-services.js` hosts (groq added `5e13372`); `tests/server/model-provider-services.test.js:26` (hosts vs pinned dist). G2 T2 model calls via vault. New 9.x providers not brokered by default (deny). | n/a (vault registry on TeamYou side; groq registered). |
| H1 | S0 | implemented (no live 9.x onboarding yet) | `ef2aedf` `onboarding/openclaw.js:39-71` `--auth-choice` ids, `:115-116` `--secret-input-mode ref\|plaintext`, env delivery of provider keys; `tests/server/onboarding-openclaw.test.js`. **Fresh 9.4/9.5 provision not run** — G2's onboarding happened on 7.1; G3 item 2. | n/a (fresh provisions only). |
| H2 | S1 | implemented (no live Codex run) | `onboarding/index.js:143` `ensureCodexPluginInstalled` (`--accept-capabilities`) before `openclaw onboard`. Not exercised in G2; W6 Codex smoke / G3. | n/a. |
| H3 | S2 | implemented — partial | `--json` passed (`onboarding/openclaw.js:119`) but the result is **not parsed** (`onboarding/index.js:329-335` awaits `shellCmd` and logs "Onboard complete"); health handled separately via `--skip-health` + `runtime-readiness.js` probe. | n/a. |
| H4 | S1 | implemented | `15ce9a8` `onboarding/workspace.js:131-146` injects the TeamYou channel step into the 9.4 Birth Sequence (`onboarding-workspace.test.js:275,293`); `teamyou-memory-activation.js:71-73,120` completion heuristics. G2 T7 PASS — but the ritual itself ran on 7.1 pre-upgrade; the 9.x template ritual on a fresh managed instance is untested (G3 fresh provision). | BOOT re-patch for workspaces whose ritual has not run (`bootstrap-kickoff.js:243`). |
| H7 | S2 | implemented | `agents/agents.js:294` `skills list --agent … --json` (W5c); `managed-defaults-config.js:43-45` `skills.workshop.autonomous.mode=propose`. G2 T7 skills PASS; Workshop `skill-collection-review-main` cron appeared after Doctor. | MIG `2026-09-apply-managed-runtime-defaults` + PKG. G2 exercised. |
| I1 | decided | implemented by omission | No `secrets configure` call; env refs + vault placeholders retained; Doctor plaintext warning observed and documented (runbook §4.2). 9.5 file-based SecretRef rule not applicable (retarget §2). | n/a. |
| I2 | decided | implemented | `managed-defaults-config.js:67-69` `secrets.egressProxy.enabled=false`; `tools.deny` += `secrets` (`lib/managed-capability-contract.json:59,73`); `managed-defaults-config.test.js:144` (pinned schema). G2 T6 PASS, T2 vault routing PASS. | MIG `2026-09-apply-managed-runtime-defaults` (ran in G2 run 1). G2 exercised. |
| I3 | S1 | implemented (live shield path unexercised) | `ef2aedf` guard rewritten (+496): refresh-lock aware `openclaw-doctor-oauth-guard.js:145-205,376-400`; tests `:357` "fails closed when an OpenClaw OAuth refresh lock is held", `:375`, `:400`, `:330`, `:480` (broker placeholders untouched). G2: guard ran in every HUS run and the T3 crash relaunch, but the instance held no OAuth profile (AI Gateway key), so shield/restore against a live 9.x refresh was not exercised. | HUS/watchdog (guard wraps every Doctor). G2 exercised (guard path), OAuth case not. |
| I5 | S1 decided | implemented | `15ce9a8` `agent-vault/service.js:509-542` `quarantineManagedChannelConfigCredentials` (`agent-vault.test.js:897`, `:801` `.bak*` sweep). G2 T5 PASS with design note: literal replaced within one tick, no proposal for an already-vault-managed slot (Bill's call on a rotation proposal pending). | BOOT (15 s vault tick). G2 exercised. |
| I6 | decided | implemented | `openclaw-runtime-env.js:35` `OPENCLAW_CONFIG_READONLY=1`; `:56` maintenance env removes only that var (`openclaw-runtime-env.test.js:54`); HUS runs Doctor via `openclaw-doctor-guard` (clawctl `:582`). G2 T4/T4A/T4C PASS (`ConfigReadOnlyError` in Control UI; Clawbridge writes OK; Doctor via guard). T9 note: refusal wording is upstream's, not "managed by TeamYou". | PKG (env). G2 exercised. |
| J1 | S2 | implemented | `cron-service.js:280-284` `skipMissedJobs`, `:375,488` `cron.runs`; UI `cron-overview.js:275`. `cron-service.test.js` updated in `15ce9a8` (no 9.4-named case found — weak). G2 T1: seed cron survived and ran on 9.4 (`cron_run_receipts=2`); Cron tab rendered. Standing approvals blocking jobs: untested. | PKG. G2 exercised. |
| J2 | S2 | implemented (deny-list semantics untested) | `gateway.js:1159-1214` `channels add/remove`; `agents/channels.js:430,560`; `channel-provider-services.js`; `agents-service.test.js:1105-1232`, `channel-provider-services.test.js`. G2 T2/T6 Telegram round trip on 9.4 (plugin loaded). `plugins.deny[]` under the 9.1 load rule (shelved WhatsApp): no explicit test. | PKG. G2 exercised (Telegram only). |
| J3 | S1 | implemented | = C4 (`routes/pairings.js:428,470`; `routes-pairings.test.js:314`). G2 T2 migrated allow entry live, T8 device approval. | = C4. G2 exercised. |

Rows not tabulated (S3, unchanged or informational): A3, B7, C3, C8, D5, D6 (`chat-ws.js:180-182` now matches by prefix), D7, E4 (`agent-vault/service.js:490,588` sweeps only root `openclaw.json.bak*`; Doctor originals untouched — W2 closeout), F6, F7, H5, H6, I4, J4.

## UNMAPPED / weakly-evidenced rows and what closes them

No row is UNMAPPED: every S0–S2 row has a mechanism in the tree. The following are weakly evidenced or
partial and should be closed or explicitly accepted before G1:

1. **A4** — run the plan's Claude-CLI continuity test (login/adoption, three turns, MCP/tool turn, Gateway restart
   without the projects symlink) and the managed-Codex `CODEX_HOME` smoke on a 9.5 instance; record which of the two
   7.1 `state/openclaw.sqlite` dirs is live after the crossing.
2. **B2 / H2 / B5** — no Codex or Claude subscription ran through G2. One Codex onboarding + `models status
   claude-cli` on the G3 beta instance closes all three.
3. **B8** — fold `gmail-watch.js:215` and `composio-install.js:270` into `writeOpenclawConfig` (they bypass the
   retired-key strip); or accept as S2 residual.
4. **C1** — the "never VACUUM/checkpoint a Gateway-owned DB without a lease" clause is met only by fail-closed on a
   busy checkpoint; either gate `physicallySanitizeAuthProfileDatabase` on a stopped Gateway / `agent_database_leases`
   or record the fail-closed behaviour as the accepted design.
5. **C2 (finding #2b)** — `bin/alphaclaw.js` still falls through to server start on an unknown subcommand; add the
   rejection so a stale host script cannot start the server in the foreground again.
6. **D1 / D3 (finding #3)** — launcher-death orphan (exit 78) leaves an unsupervised Gateway; re-test T3 crash on
   9.5 (restart semantics changed) and decide adopt-vs-force-stop.
7. **H1 / H4** — no fresh 9.x onboarding has run: `--secret-input-mode ref` auth branch and the injected Birth
   Sequence step are unit-tested only. G3's fresh beta provision (retarget §6.2) is the closing evidence.
8. **H3** — `--json` output is not parsed; either parse the consolidated result or downgrade the row to "exit status
   + `--skip-health` + own readiness probe" and mark done.
9. **G1** — assert on an upgraded instance that the 7.1 catalog cache is invalidated (T11 "begin with a prior
   AlphaClaw cache" was not explicitly recorded).
10. **I3** — exercise the guard's shield/restore against a real OAuth profile under 9.5's refresh locks (a Codex or
    Claude-login instance at G3).
11. **F5** — add `assets/icon.png` to `usage-tracker` and `agent-vault` (cosmetic); check async-storage deprecation
    warnings on 9.5.
12. **J1 / J2 / D8** — no named 9.x test cases for cron payload shapes, `plugins.deny` under the 9.1 load rule, or
    the 30/min RPC ceiling; live behaviour passed T1/T2/T6. Add focused cases or accept.

## Delivery-path gaps that apply across rows (block G3/G4, not G1)

- The host upgrade script is absent from the stable host bundle `4c6e717d`; TeamYou-provisioned hosts need it copied
  to `/root` (G2 runbook §4) until clawctl ships it.
- The stable bundle's `teamyou-install.sh` re-writes `agents.defaults.memorySearch` / `plugins.bundledDiscovery` every
  5 min (finding #1) and the state backup export fails on 2026.9 lock files (finding #8). Both fixed on clawctl
  `455ed6e` / `40696a4` / `78d0480` (bundles `28d50d60`, `fed9a46b`, `f62fa09f`), pinned on preview beta only. Every
  existing managed instance must receive the patched library and export script (or a promoted stable bundle) as part
  of its per-instance runbook before or with the 9.5 install.
- The 9.4 → 9.5 step (agent DB schema 21) has not run on any instance; G2's crossing evidence is 7.1 → 9.4 only.
