# Egress Program Status

**Updated:** 2026-08-25 — maintained by Claude; ask for "the egress status board" in any session.

## Live infrastructure

| What | Where | State |
| --- | --- | --- |
| `alphaclaw-egress-enforced-1` (+ gateway) | Dedicated test Hetzner project | **Enforced**, onboarded, healthy. Dashboard: `https://alphaclaw-egress-enforced-1.tail2cd802.ts.net/` (tailnet-only; password in Doppler `SETUP_PASSWORD__INST_8ADB16F0…`). **No time required from Bill**: Claude drives a scoped agent-exercise session (web search, browsing, memory — no external accounts) via the gateway's private path to generate the Phase 3 flow-log inventory. |
| `alphaclaw-egress-soak-1` (mediated) | — | Destroyed 2026-08-23; teardown test passed clean. Residue: dead node(s) in the tailnet admin console (manual cleanup, cosmetic). |

## Threads

| Thread | State | Owner | Next action |
| --- | --- | --- | --- |
| Egress Phases 0–2 (spec, spike, mediated, enforced flip) | **Done & live-validated** on branches | — | — |
| clawctl egress branch | **Merged to `main`** 2026-08-24 (0fb72df, 431/431 tests + tsc clean on merged main) | — | — |
| teamyou PR #791 (Phase 1+2) | **Merged** 2026-08-24 (admin-merge per owner direction) | — | — |
| Attestation workflow | **Merged** to `development` 2026-08-24 (PR #798). Phase 2 code is fully landed | **Bill** | On next production deploy: run `{"mode":"once"}` via POST /api/internal/openclaw/egress-attestation (expect clean empty sweep), then start the chain — that closes Phase 2 |
| Customer incident (milo-b0171223) | **Root cause confirmed on-box 2026-08-25** (temporary IP-scoped SSH firewall, since removed; host keys verified against DB). Single trigger: onboard POST took ~80s with zero response bytes, and the gateway bridge's Caddy `response_header_timeout 60s` cut it → customer saw "interrupted" (symptom 1) → and because host finalization hangs on the response `finish` event, **finalization never ran**: journal shows kickoff "deferred… restart pending" at 23:46:35 then "gave up" at 00:01:25 (symptom 2), and `alphaclaw-setup.service` is STILL the running unit. **The instance is half-finalized**: running with `NoNewPrivileges=false`, finalize sudoers still present, runtime service disabled, post-onboard reconcile timer never enabled. Symptom 3 is the independent vault-env bug (fixed in beta.2). Fleet check: milo is the only affected instance (other two gateway-topology instances are our test boxes; pre-July instances predate the bridge) | **Bill** | **Remediated 2026-08-25 06:06 UTC** (owner-approved): ran `alphaclaw-host-finalize-setup complete` — runtime unit active with `NoNewPrivileges=yes`, setup unit disabled, reconcile timer enabled, finalize sudoers removed; ~5s actual downtime; kickoff correctly decided `bootstrap_already_complete` (no spurious greeting); both customer surfaces verified 302/200 with real Host headers; temp SSH firewall removed again. Remaining for milo: promote beta→latest, then run an **operator-driven upgrade** — **corrected 2026-08-25: managed instances have NO self/auto-update path**. `POST /api/alphaclaw/update` ("self-update") is user-clicked, self-hosted-only, and pulls `latest`; on clawctl instances the dashboard shows "contact TeamYou support" and the host reconcile timer only reconciles the TeamYou plugin. Delivering fixes to milo means a manual npm install + restart over the gateway path (or a TeamYou-side upgrade flow, which does not exist yet — candidate clawctl work item) |
| Finalize-hangs-on-response-delivery bug (alphaclaw) | **Fixed + released 2026-08-25** in `0.9.18-starfoundry.18-beta.3` (c118b88): finalization now runs on response `finish` OR `close`, with a 10s fallback timer for sockets that died before the handler completed (milo's exact case). 3 new unit tests; 1251/1251 suite green. Beta dist-tag now beta.3 | Bill | Promote to `latest` with the rest of the .18 line after beta validation |
| Bridge `response_header_timeout 60s` too short for onboarding (clawctl + teamyou) | **Fixed in both repos 2026-08-25**: 60s→300s in `connectivity-caddy.ts` — clawctl main (419b795, 431/431 tests) and teamyou development (PR #804, merged; 66/66 provisioning tests). The timeout is a ceiling, not a wait. Rendered at provision time, so **future provisions only** — milo's live gateway still carries 60s (only long dashboard actions through the bridge could notice; hot-patch available on request) | — | Done for new provisions |
| Setup-completion readiness gate | **Shipped by Bill** in `0.9.18-starfoundry.18-beta.1`; carried forward in beta.2 | **Bill** | Live test: fresh TeamYou-admin provision with channel=beta |
| Flow-log inventory report (Phase 3 input) | **DONE 2026-08-26** — `docs/egress-flow-log-inventory.md` (artifact 962889e5…). 2.3-day window, 667 flows/1.73 GB. Headlines: agent exercise (search/fetch/memory) produced ZERO direct flows (vault transit proven); DNS + Tailscale bypass the NAT via the workload public interface (source-policy) — gateway has NO resolver today, Phase 3 must add a DNS forwarder before retiring public ingress; zero plaintext/port-22; native browser fetches direct-dial (the Tier 2 case, observed live Aug 24); 9 channel direct-dial SYNs (telegram/discord) only inside the Aug 25 restart-storm window — reproduce-on-restart follow-up before closing; alphaclaw server probes (catalog/teamyou/releases) dial direct by design, vault-fetch candidates. Steady-state allowlist ≈ 6 destination classes. Gateway reached via temp own-IP firewall (created+removed same session, verified closed) | Claude | Phase 3 planning can start; follow-ups listed in the report |
| Host-asset bundle env bump in teamyou Vercel (`ed1ac144`) | **Deliberately deferred** (owner call 2026-08-24): a customer is mid-onboarding; bump waits until the coast is clear. (Note: the bundle URL/SHA is read once at stage-zero claim, so in-flight instances keep their bundle — the bump only affects future claims. Recorded so the timing decision has full context.) | Bill | Bump when comfortable; required before TeamYou-provisioned mediated/enforced instances |
| Loopback-through-proxy class (web_search et al.) | **Released to beta 2026-08-25** in `0.9.18-starfoundry.18-beta.2` (shim fadc521, live-validated earlier on the enforced instance). Vault plaintext-sniff rejected; no upstream change needed | Bill | Promote to `latest` after beta validation — that's what reaches milo |
| Mediated route lost on DHCP renewal | **Fixed 2026-08-24** (clawctl 2a27d05): bootstrap writes a netplan drop-in so networkd owns the route + policy rule; the old oneshot became a probe/repair alarm on a 5-min timer ("alphaclaw-egress-alarm" journal token). Live-applied to the enforced instance and proven: route survives forced networkctl reconfigure; strip-and-repair drill passes | Bill | Rides the branch merge; bundle republished as ed1ac144 |
| Vault-env plugin-install bug | **Released to beta 2026-08-25** in `0.9.18-starfoundry.18-beta.2` (6b769a2). Live test instance was manually unblocked earlier | Bill | Promote to `latest` after beta validation |
| Onboarding recovery-poll window | **Fixed + released to beta 2026-08-25** (3ac11e8, in `0.9.18-starfoundry.18-beta.2`): recovery poll extended 36s→120s to outlast the ~55s service-swap dark window; a retried submit answering "Already onboarded" now resolves through the status poll into the normal readiness handoff. Guard test pins poll ≥ 2× the observed window. 1248/1248 tests | Bill | Promote to `latest` after beta validation |

## Threads (continued)

| Thread | State | Owner | Next action |
| --- | --- | --- | --- |
| Channel ingress/egress audit under dual-VPS | **Assessed 2026-08-25** (owner concern): all channel transports are outbound (Discord WSS+REST, Slack Socket Mode — `SLACK_APP_TOKEN` is the socket-mode credential, Telegram long-poll, WhatsApp WSS, signal-cli sidecar), so mediated/enforced egress **cannot break them** — NAT routes proxy-unaware traffic by design; Discord voice UDP masquerades fine. **Verified in `@openclaw/discord@2026.7.1`**: the gateway WSS direct-dials unless a per-channel `channels.discord.proxy` is set; the managed vault proxy is never consulted → channel sockets are invisible to the vault (no brokering, no future Phase-3 scanning of those flows). Key find: the per-channel proxy hook routes BOTH the WSS and REST through an explicit proxy — alphaclaw can point it at the vault (via shim) with **no upstream openclaw PR**; other providers' plugins unverified. Inbound-webhook modes (Telegram webhook mode, Gmail push — already broken under strict routing) are the only ingress surfaces; recommend standardizing on outbound-only modes in Phase 3. **Superseding refinement 2026-08-25**: the pinned openclaw embeds `@openclaw/proxyline` (managed mode when `proxy.enabled`) — process-global routing that covers node http/https, undici/fetch, and caller-supplied agents, re-installed in child processes; so Telegram long-poll (grammY/fetch) and Slack REST are expected to transit the vault proxy already — Discord's private transport is the one confirmed direct-dialer. Folded into `docs/vault-brokered-channels-spec.md` §2/§8 | Claude | Empirically confirm Slack/Telegram transit during the Phase A checklist on the enforced instance |
| Model/gateway API keys vs Agent Vault | **Phases A+B implemented on `main` 2026-08-25** (f10c6fb, owner-approved; spec `docs/vault-brokered-model-keys-spec.md`): 24-provider service registry (hosts pinned to openclaw 2026.7.1), `POST /api/models/vault-key` + `ensureModelProviderAccess` (one merged proposal per provider, multi-host incl.), reconcile flips raw env/auth-store values to `__agent_vault_*__` placeholders once vault has service+credential, raw keys hard-rejected on models routes after runtime claim (envars route already blocked them), models UI card = store/migrate button → pending-proposal poll (decline-safe) → passive "Agent Vault" state. Verified end-to-end in the UI sandbox (15s auto-approve mock); 1270/1270 tests. §9 defaults taken: hard-block, UI-initiated migration, env-var slot names, shared plan-variant services, multi-key deferred | **Bill** | Release to beta, then live-validate on `alphaclaw-egress-enforced-1` (configure Anthropic via vault flow; §10 checklist). Phase C (AGENTS.md doctrine, pairs with the pending channel carve-out) and Phase D (provision-time seeding) open. **Registry gap closed in `beta.14` 2026-08-25** (owner spotted a configured Vercel AI Gateway key with no move-to-vault option): the six providers the spec parked as verify-at-impl — vercel-ai-gateway, cloudflare-ai-gateway, fireworks, venice, kilocode, tencent-tokenhub — are now in the registry with hosts confirmed against the pinned openclaw dist/docs (all fixed-host verbatim-header; Cloudflare's account ids ride the path; Tencent's two regional hosts use the minimax pattern; the `vck_` prefix rule already had the placeholder exemption). groq/cerebras stay parked (no catalog env key). Spec §4 table updated. **`beta.15`** (installed): owner found the migrate flow was a dead end — the approval page asks for the key, but the migrate UI had replaced the key input (and its Show toggle) with the proposal card, so the on-server value was unretrievable; the migrate row and pending-proposal state now both offer a Copy key button (browser-verified both states; affordance disappears once only the placeholder remains). A registry-drift guard test also landed (f6330b9): catalog providers with env keys must be in the vault registry or on an explicit exclusion list. **Acceptance testing CLOSED 2026-08-25**: owner completed the Slack pairing walk (validates the pairing-poll fix live) and signed off on the Vercel key migration; owner waived throwaway-token cleanup. **Phase D shipped in `beta.16`** (installed; live AGENTS.md on the instance verified to carry it): "Clawbridge-managed credentials" doctrine section — model/gateway keys → Models screen, channel tokens → dashboard channel setup, Tier C custody statement, and placeholders-are-working-values guidance — closing the doctrine conflict behind the milo Discord 401 class. Channels program alphaclaw-side COMPLETE. **Phase E (Tier C sealed custody) implemented + live-validated 2026-08-26** (clawctl main 811ae01, 432/432 + tsc clean; owner-directed): gateway-held KEK with a dedicated restricted forced-command SSH channel (never self-seals, unlike the setup channel), workload seal/unseal CLI + boot unit in the host asset bundle, verification checks both sides. Live drill on the test pair: seal/tmpfs/ciphertext ✓, boot re-unseal ✓, KEK-denial leaves only ciphertext with the instance healthy ✓, recovery ✓; custody channel left deployed on the pair (temp gateway firewall removed, verified closed). ENTIRE channels program now closed except the future alphaclaw hook: call `seal` from the channel-add flow when WhatsApp/Signal first ships; host-asset bundle republish (new ed1ac144 successor) still pending the deferred Vercel env bump |
| Host-asset bundle release channels (beta soak without exposing customers) | **Implemented 2026-08-26** (owner-directed, designed in-session). clawctl main 56065c0 (435/435 + tsc clean): every published bundle is provenance-stamped (commit/branch/dirty — in the artifact as `alphaclaw-bundle-provenance.json` installed to `/root/`, and in the manifest); `host-assets:publish` refuses dirty trees without `--allow-dirty` and marks builds stable-promotable only when clean AND on origin/main OR descending from the previously published bundle's commit (admits pinned-baseline hotfix branches, keeps stray builds beta-only); publish output prints the per-channel TeamYou env pin lines. teamyou [PR #813](https://github.com/starfoundrystudio/teamyou/pull/813) (11/11 tests + tsc clean): `getHostAssetBundle(channel)` — beta provisions use `OPENCLAW_HOST_ASSET_BUNDLE_URL_BETA`/`_SHA256_BETA` when both set, all else stable; `latest` can never receive the beta bundle. Model: build once → soak on beta pin → promote the identical sha; hotfix flow documented in clawctl `docs/host-asset-release-channels.md`. Live-verified provenance+guard against the real repo (main tip promotable, sha b78ef452…) | **Bill** | Merge PR #813, then the deferred Phase E bundle publish goes to the **beta pin first** — coworker beta provisions soak it before the stable pin moves (this replaces the riskier single-pin Vercel bump). **Bundle-publish blockers cleared 2026-08-26**: (1) clawctl bootstrap now treats an unstaged custody key as a loud skip instead of a dead provision (clawctl main b5481a9, 435/435; custody CLI/unit still install so a later key drop lights it up) — the Phase E bundle can no longer kill provisions from a custody-unaware provisioner; (2) [PR #814](https://github.com/starfoundrystudio/teamyou/pull/814) ports Phase E provisioning to TeamYou's own path (custody keypair in its PKI, staging both sides, vendored forced command + KEK in its gateway installer; 16/16 tests + tsc clean) so beta provisions exercise custody end-to-end. Remaining order: merge #813 + #814 → publish bundle (inert) → set the `_BETA` pins → coworker beta provisions soak → promote the same sha |
| WhatsApp channel is a dead end under enforced vault (owner question) | **Verified 2026-08-26 on the enforced instance**: WhatsApp is *configurable* (owner number saved, `@openclaw/whatsapp` auto-installs, allowlist + classification gate pass) but **cannot link** — `channels login` never emits a QR; the WhatsApp Web socket dies "before fully opening". NOT transport (CONNECT/TLS-with-MITM-CA/`wss` 101 upgrade all succeed through the vault) — it's WhatsApp's **Noise-protocol handshake inside the WebSocket**, which the vault MITM breaks (Noise authenticates the peer independently of the intercepted TLS). Side effect: pairing never completes, so **Phase E custody is untested against a real WA keystore**. Test channel created + deleted cleanly; instance healthy; env/config clean. Full write-up in `docs/vault-brokered-channels-spec.md` §4. **RESOLVED 2026-08-26: owner chose to shelve it** — `beta.17` (installed + live-verified): WhatsApp removed from the channel wizard + pairings hint, added to `plugins.deny` by reconcile even though classified (live-confirmed on the instance), and the channel-add API rejects it with a clear message (live-confirmed). Stays Tier C classified with custody notes; `@openclaw/whatsapp` plugin + login routes untouched so self-hosted CLI adds still work. Single `kShelvedChannelPluginIds` entry to lift when the passthrough is built | — | Reopens only if/when the option-(a) `web.whatsapp.com` MITM passthrough is built — which is also the prerequisite to test Phase E custody against a real WA keystore |
| Channel credentials vs Agent Vault doctrine conflict | **Diagnosed 2026-08-25**: milo's Discord 401 loop — channel tokens are env-based by design (channel UI → `.env` `DISCORD_BOT_TOKEN` → openclaw env ref; `token:env` is correct), but `AGENTS.md` tells the agent "Agent Vault is mandatory / never write credentials to Envars", so the agent directed the customer to put the reset token in the vault, where nothing reads it for channels. Stale env token → 401 forever. Milo unblock: reset token once more, enter it in AlphaClaw channel settings (not vault), restart. Stopgap: AGENTS.md carve-out routing channel tokens to channel settings. Real fix: vault-broker channel tokens. **Spec drafted 2026-08-25** (`docs/vault-brokered-channels-spec.md`, owner-requested before the next beta): reuses the model-keys machinery (placeholders, registry, proposal flow, reconcile) with two extensions — multi-slot services (Slack's two tokens, one approval) and per-account slots (`DISCORD_BOT_TOKEN_<ACCOUNT>`). Transport: Telegram is pure config (path substitution; grammY transits via proxyline); Slack expected zero-transport (secrets ride REST; WSS is ticketed, plugin transit to verify); Discord needs `channels.discord.proxy: "${OPENCLAW_PROXY_URL}"` (env-ref keeps the git-synced config secret-free) + header/websocket substitution. Alphaclaw's own channel probes (telegram/discord/slack-api, watchdog-notify) gain a vault-aware fetch. Kills the token-readback endpoint's secret exposure. **v2 same day** (owner rejected exclusion-by-fiat): full credential taxonomy replacing exclusions — Tier S (substitutable tokens), S/te (OAuth client secrets via token-endpoint body substitution — the msteams/Bot Framework path), D (derived ephemerals, memory-only), C (in-process crypto material: WhatsApp/Signal keystores, LINE HMAC, Nostr keys — honest statement that runtime confidentiality from the agent is unachievable; C0 custody hygiene now, C1 sealed-at-rest via gateway-held KEK or a future vault unwrap primitive), L (locally-terminated hosts). All 26 catalog channels + core 4 classified; governance rule: unclassified channels can't be enabled on managed instances; 5-question classification checklist with msteams as the worked example. 5 phases (E = Tier C sealed custody, decoupled). **All 6 open questions decided by owner 2026-08-25** (spec §9 D1–D6): gateway-held KEK · hard block unclassified · per-account slots · inbound webhooks stay supported via the Tailscale-funnel path (msteams unblocked; Composio triggers are the precedent; verify each webhook channel against the known strict-routing 404 class) · probe failure blocks the flow · deny-list default-closed. §5a added same day: enforcement outside Clawbridge via gateway-side `plugins.deny` (checked before entries.enabled and the bundled-channel allowlist bypass — binds Control UI/agent/CLI), reconcile re-assert, raw-secret sweep | Claude | **Phase A transit verification COMPLETE across all three channels 2026-08-25** (real test tokens; instance restored clean each run; spec §8 has full results). Telegram: real-token channel + long-poll fully via shim→tunnel, zero direct 443. Slack: "socket mode connected" — REST + ticketed WSS via the vault, zero direct 443. Discord: **supersedes the audit** — proxyline intercepts the plugin's WSS beneath its transport, so even without the per-channel hook nothing direct-dials; with `channels.discord.proxy:"${OPENCLAW_PROXY_URL}"` the env-ref expands and both "rest/gateway proxy enabled" hooks engage; WSS stable through the vault MITM (socket-table proof + no-channel control). Also: gateway restart-loop breaker (≥3 unclean boots/5min) suppresses channel autostart — Phase B restart choreography constraint; approval links must always be TeamYou entry-hop wrapped (raw operator login is a dead end on managed instances); earlier items 4–6 findings stand (probe-free adds, allowlist-refusal live, `${VAR}`-literal-on-empty, `openclaw.json.bak*` retention). **PHASE A COMPLETE 2026-08-25**: after owner approval of proposals #1–#3 (entry-hop links; raw operator links are a dead end — UX note for the vault team), the substitution end-to-end passed for all three channels with **only placeholders on-box** (secret scan clean; path/header/websocket surfaces all proven; Discord IDENTIFY inferred from stable authenticated socket + no 4004 crash loop). The test vault retains the canonical service defs + test credentials. **Phases B+C IMPLEMENTED on `main` 2026-08-25** (0150048 server, 175610e wizard; owner-approved): channel classification registry + D6 deny-list derivation/re-assert · `ensureChannelProviderAccess` (multi-slot, per-account, one approval) · `POST /api/channels/vault-token` · createChannelAccount classification gate + raw-token rejection + D5 post-approval probe via a CONNECT-tunnel vault-aware fetch (placeholder shape exemptions in inspectors) · discord proxy env-ref with runtime symmetry · reconcile placeholder flip + raw-secret sweep incl. `openclaw.json.bak*` · update-route raw-rotation rejection · wizard vault flow (store → entry-hop approval → active placeholders → connect), edit mode shows vault state. 1289/1289 tests; wizard lifecycle browser-verified in the sandbox | **Bill** | **`0.9.18-starfoundry.18-beta.4` released 2026-08-25** (0878f11, beta dist-tag moved, `latest` untouched, main pushed) **and manually installed on the enforced instance** (service healthy; first reconcile wrote the D6 deny-list live: 23 unclassified catalog channels denied, classified 4 untouched). **Acceptance tests 2026-08-25: Telegram (instant-active path) and Discord (fresh-proposal path incl. real TeamYou approval) both PASSED.** Two findings from the Discord run: (1) pending-pairings polling disarmed before the first DM arrived (gate required already-pending requests) — **fixed + released in `beta.5`** (92d83bc; poll now runs whenever an unpaired channel awaits pairing; beta.5 installed on the enforced instance); (2) the pairing DM tells the end user to run `openclaw pairing approve <ch> <code>` — hardcoded in openclaw `buildPairingReply`, no config hook → upstream-only fix, **parked by owner decision 2026-08-25** (see parked list). Third finding (Slack run): connected-screen **deep-link buttons vanished in vault mode** (they derive from client-side verification results the vault flow skips, while the copy still promised them) — **fixed + released in `beta.6`** (0877120: inspect endpoints are vault-aware for placeholders, wizard fetches identity post-connect; browser-verified — "Message \<bot\> in Slack" renders; beta.6 installed on the enforced instance). Fourth finding (Slack re-add on beta.6): the Slack conversation deep link degraded to the generic workspace URL — the app id is normally parsed **from the xapp token string**, impossible with a placeholder, and bot `auth.test` has no `app_id`. **Fixed + released in `beta.7`** (2555b4b: fall back to `auth.test` with the app-level token, which returns `app_id`, substituted at the proxy; live-verified on the instance — placeholder-only inspection resolved the real app id). Fifth finding + owner-directed UX fixes, **released in `beta.8`** (3336a18, installed on the instance): vault mode had dropped the entire guided setup — including the Slack manifest (whose absence caused the owner's disabled-Messages-tab app) and Discord intents guidance — now restored via a `vaultMode` prop that keeps guidance and swaps only token entry for the vault block; Discord gains a static intents checklist in vault mode; connected-screen deep-link buttons no longer embed the bot name (wrapping). `beta.9` (installed) adds the Discord DM deep link ("Open in Discord" → bot profile URL; a mutual server is Discord's own DM precondition). `beta.10` (installed) restores the full Discord post-verify experience in vault mode — the vault-active wizard now auto-fetches the bot identity through the vault-aware inspect, lighting up the live intents card, "Recheck intents", and the **preconfigured "Add bot to a server" install URL** (all previously gated on client-side verification vault mode skipped); with identity loaded, the raw-flow intent/server gates apply in vault mode too, and the connected screen warns with the install link when the bot has no server. `beta.11` (installed): owner-directed UI pass — all user-facing copy rebranded AlphaClaw→Clawbridge (30 files, display strings only; identifiers/package unchanged; Slack manifest defaults follow) and the channel wizards de-jargoned/tightened without losing guidance (vault proxy-mechanism talk removed, note boxes folded into numbered steps, Discord permission enumeration dropped, connected checklists shortened). `beta.12` (installed): setup guidance cards collapse once the bot identity is verified (Discord modal roughly halves; applies to all three providers), Slack gains a terse stash-each-token tip for the two-token approval, and a missed Discord footer got the copy diet. `beta.13` (installed): owner spotted "AlphaClaw" in the live vault proposal text — the beta.11 rebrand only swept the dashboard UI, so ALL server-built copy was missed; full sweep of lib/ (vault proposal reason/userMessage, watchdog alerts, chat + UI error strings, doctor prompt, AGENTS.md/TOOLS.md core prompts, Tailscale + Google-console resource descriptions, gateway device display name, state-repo commit message), chat system-note prefix now `[Clawbridge]` with the legacy `[AlphaClaw]` prefix still transcript-hidden, and a new branding guard test fails the suite if the retired token reappears under lib/ (note: already-created proposals keep their stored old-brand text; only new proposals get the new copy). Remaining: finish the Slack pairing walk (validates the pairing-poll fix live), revoke throwaway tokens, Phase D doctrine |

## Explicitly parked (no one is working on these)

- Onboarding speed + async redesign (owner concern 2026-08-25): milo's ~80s breakdown — 5s git/onboard, 22s `models set`, 37s skills/git-sync installs, 15s tailnet+vault. Two-part plan when picked up: (a) defer non-blocking housekeeping to the post-onboard reconcile timer (~20–30s critical path), (b) convert POST /api/onboard to a background job + progress polling so no long-lived response exists for any hop to cut (makes the bridge-timeout bump unnecessary and finalize-decoupling trivial).

- Phase 3: retire public bootstrap ingress; transparent payload scanning (PipeLock); egress policy tiers — needs the flow-log inventory first.
- Phase 4: OpenClaw privilege escalation (sudo/power-mode) — gated on attestation running fleet-wide.
- alphaclaw `enforced`-mode observability (doctor/watchdog "gateway unreachable" state) — downgraded from prerequisite to nice-to-have by the Phase 2 NAT correction.
- Existing-instance migration — out of scope by owner decision (2026-08-21); new instances only.
- Tailnet dead-node cleanup on instance destroy — cosmetic, unscheduled.

- Pairing DM approve-hint wording (owner decision 2026-08-25: not pursuing upstream now). The DM's "Ask the bot owner to approve with: `openclaw pairing approve <ch> <code>`" is hardcoded in openclaw `buildPairingReply` (no config surface; alphaclaw cannot intercept). When picked up: upstream PR making the hint line configurable (e.g. `gateway.pairing.approveHint` with `{channel}`/`{code}` placeholders, CLI text as fallback) so managed instances can point at the dashboard's Pending Pairings instead — remember the message addresses the end user, not the owner, so no tailnet-only links in the text.

## Chips / tickets

- No pending chips. (The readiness-gate chip was completed by Bill; its two predecessors were withdrawn as superseded.)
- Nothing filed in Linear for this program, deliberately.
